using NUnit.Framework;
using Moq;
using System.IO;
/*using EasySave.Models;


[TestFixture]
public class CompletBackupTests
{
    private CompletBackup _completBackup;

    [SetUp]
    public void Setup()
    {
        // Initialisation de la classe CompletBackup
        _completBackup = new CompletBackup();
    }

    [Test]
    public void TestCopyFilesAndDirectories()
    {
        // Test de la copie de fichiers et de répertoires du répertoire source vers le répertoire cible
        // Écrire le code du test ici
    }

    [Test]
    public void TestSourceDirectoryNotFound()
    {
        // Test du comportement lorsque le répertoire source est inaccessible ou n'existe pas
        // Écrire le code du test ici
    }

    [Test]
    public void TestTargetDirectoryCreationFailed()
    {
        // Test du comportement lorsque le répertoire cible ne peut pas être créé
        // Écrire le code du test ici
    }

    [Test]
    public void TestBackupProcessStopped()
    {
        // Test du comportement lorsque le processus de sauvegarde est arrêté
        // Écrire le code du test ici
    }
}
*/